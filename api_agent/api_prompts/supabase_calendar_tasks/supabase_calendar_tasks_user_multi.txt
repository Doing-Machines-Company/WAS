Existing future events:
${future_events}

Existing future tasks:
${future_tasks}

API output:
${task}

Current date/time (UTC) is:
${current_datetime}

First follow all the reasoning steps using chain of thought, step-by-step. Then produce valid JSON inside triple backticks.