Here is your action memory so far:
$memory

Currently available calendars:
$available_calendars

Currently available task lists:
$available_tasklists

User's main request:
$task

Your goal is to interpret the items from this **external API** (in `$task`), decide which belong on Google Calendar (fixed date/time), which belong on Google Tasks (to-do items with optional or flexible due dates), and which items don't have associated dates and should be ignored, then take the appropriate actions.
Reason step-by-step using Chain-of-Thought (CoT) to do so.

Remember:
- If you need to fetch more information first (e.g., listing tasks or events to check duplicates), do so with **one** action.
- Once you have all necessary information, finalize your plan with a single `"STOP"` action and a `"final_answer"` array containing all sub-actions to be performed.
- Make sure to provide times in **RFC 3339** format if applicable.
- Remember you can only use exactly **one** action per response, unless using the final `"STOP"`.

Now, provide your action in valid JSON (triple backticks). If no parameters are needed, use `"parameters": {}` or `"parameters": null`.