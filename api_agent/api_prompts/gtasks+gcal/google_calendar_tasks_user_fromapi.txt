All future events (unrolled from all calendars):
$future_events

All future tasks (unrolled from all tasklists):
$future_tasks

API Output:
$task

You can:
- Check these existing future events/tasks to avoid duplicates.
- Create new events only on the **"inbound_fyi_calendar"** calendar.
- Create new tasks only on the **"inbound_fyi_tasklist"** tasklist.
- Once you have enough info, finalize with a single `"STOP"` that has a `"final_answer"` array of sub-actions.

Please reason step-by-step using chain-of-thought to complete the user’s request, **then produce only your final** JSON action in valid format (triple backticks). 
Remember:
1. Output valid JSON inside triple backticks.
2. Only one `action_type` per response, unless final `STOP`.
3. Check carefully for duplicates before adding a new item.
4. If no parameters needed, use `"parameters": {}` or `"parameters": null`.
5. All times/dates used must be in RFC 3339 format, we always work off of UTC time so you don't have to specify a timezone.
6. Do not add datetime information inside descriptions/titles/notes.

