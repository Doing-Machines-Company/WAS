All future events (unrolled from all calendars):
$future_events

All future tasks (unrolled from all tasklists):
$future_tasks

User's main request:
$task

You can:
- Check the existing future events/tasks to avoid duplicates.
- Only create new events on the "inbound.fyi" calendar.
- Only create new tasks on the "inbound.fyi" tasklist.
- Use the local time for tasks in the title (plus UTC in notes) if they have a specific time.
- If you need more info, do a single step like `CALENDAR_LIST_EVENTS` or `TASKS_LIST_TASKS`.
- When you have everything, finalize with a single `"STOP"` containing a `"final_answer"` array of sub-actions.

Reason step-by-step using Chain-of-Thought (CoT) to complete the user's main request.
Output your action in **valid JSON** within triple backticks. Only **one** `action_type` unless final `"STOP"`.

Remember:
1. Output must be valid JSON enclosed by triple backticks.
2. Check carefully for duplicates in both events (future) and tasks (uncompleted or relevant to the user’s chosen list).
3. If no parameters are needed, return `"parameters": {}` or `"parameters": null`.
4. All times and dates used should be in RFC 3339 format, we always work off of UTC time so you don't have to specify a timezone.